import 'package:flutter/material.dart';

class ProjectDimensions {
  // BorderRadius
  static const BorderRadius borderRadius =
      BorderRadius.all(Radius.circular(16));
  static const BorderRadius buttonBorderRadius =
      BorderRadius.all(Radius.circular(100));
  static const Radius scrollBarRadius = Radius.circular(100);

  // Padding
  static const EdgeInsets textPadding = EdgeInsets.all(8);
  static const EdgeInsets containerPadding = EdgeInsets.all(16);
  static const EdgeInsets pagePadding = EdgeInsets.all(28);
}
